version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
      - cypress/run:
          start: npm start
          record: true
          parallel: true
          parallelism: 2
          group: 2 machines
